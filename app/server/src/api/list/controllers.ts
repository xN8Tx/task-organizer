const controllers = {
  signin: async (req: Request, res: Response) => {},
  registration: async () => {},
  getMe: async () => {},
  refresh: async () => {},
  logout: async () => {},
};

export default controllers;
